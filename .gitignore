# Secrets
.env
*.key

# Data / cold store (never commit market data)
# NB: anchored to repo root — a bare "data/" would swallow src/signals/data/
*.duckdb
*.sqlite
*.sqlite3
/data/
*.parquet

# Runtime logs & state (status.json, alert_state, research/digest history) —
# these live on the server only; deploy is `git reset --hard`, never commits them.
/logs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Notebooks / analysis
.ipynb_checkpoints/

# OS / editor
.DS_Store
.vscode/
.idea/
/reports/
